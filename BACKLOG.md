# Backlog — Enertika Ops Core

Items diferidos. NO implementar sin decisión explícita del equipo.

---

- **Levantamientos — Mapa de sitios:** integración Google Maps para visualizar ubicaciones de levantamientos.
- **Dashboards / Alertas — Fase 6:** dashboards operativos y sistema de alertas configurable.
- **BOM SSE:** notificaciones en tiempo real (Server-Sent Events) para cambios de estatus en BOM.
- **Traspasos — Badge status en tab Ganadas (Comercial):** mostrar indicador de traspaso/proyecto en cards del tab Ganadas. `templates/comercial/partials/cards.html`.
- **Traspasos — UI polish:** contador de docs en modal, tooltip días en área, filtros URL en proyectos.
- **Compras — Bug OOB stats staging modal:** vincular XML muestra ceros hasta F5. Triple nesting de `<div id="stats-container">` en `estadisticas.html` + `content.html` + `router.py:392`. Fix: quitar el wrapper extra en `content.html`.
- **PDF Simulación — Gráficas automáticas:** `ReportesSimulacionService.get_graficas_pdf()` no existe. El endpoint `/pdf/generar-automatico` pasa `charts={}`. Implementar para que el PDF incluya gráficas vía QuickChart (`core/charts/service.py`).
- **`tb_usuarios.es_rh` — columna muerta:** se carga en `core/security.py` (`context["es_rh"]`) pero ningún módulo la consume. El acceso real a RH/vacaciones ya usa RBAC estándar (`user_has_module_access("rrhh", ...)` en `modules/vacaciones/router.py:380`). Verificado en PROD: el único usuario con permiso sobre el slug `rrhh` tiene `es_rh=false` — confirma que están desincronizados. No expuesta en Admin UI. Candidato a eliminar (columna + carga en `core/security.py` + migración de drop) tras confirmar que no hay otro consumidor oculto.
- **BOM — E1: batch query rc+coordinador en `enviar_revision_ing`:** `core/bom/service.py` ~l.513 hace dos `get_asignacion_proyecto` secuenciales para `responsable_construccion` y `coordinador_obra` del mismo proyecto. Se puede unificar en un solo query `WHERE rol_proyecto = ANY(ARRAY[...])`. Requiere nuevo método `get_asignaciones_proyecto(conn, id_proyecto, roles: list[str], area: str) -> dict[str, dict]` en `core/bom/db_service.py`.
- **BOM — E2: query duplicado `ingeniero_asignado` en `bom_ui`:** `core/bom/router.py` ~l.187 llama `get_asignacion_proyecto(..., "ingeniero_asignado", "INGENIERIA")` dentro del bloque `if not bom:`, pero `service.puede_crear_o_retomar_bom` (llamado justo antes) ya ejecuta la misma query internamente. Fix limpio: cambiar el return type de `puede_crear_o_retomar_bom` para que devuelva también la fila del ingeniero, evitando la segunda ida a BD.
- **BOM — Fragilidad `_validar_aprobador_bom` con `fallback_rol_org=None`:** `core/bom/service.py`. Cuando `solo_responsable=True`, `responsable_id=None` y `fallback_rol_org=None`, la función retorna sin ejecutar ninguna validación — cualquier usuario quedaría autorizado. Todos los callers actuales pasan un fallback, pero el contrato es frágil. Fix: agregar `assert fallback_rol_org is not None` o levantar `ValueError` explícito al inicio si ambos son None.
- **BOM — Router llama `service.db` directo en `bom_ui:187`:** `core/bom/router.py`. El check `get_asignacion_proyecto("ingeniero_asignado")` se hace desde el router saltando la capa de servicio. Encapsular en un método de `BomService` (ej. `service.validar_ingeniero_asignado(conn, id_proyecto)`) para que el router no dependa de `db_service` directamente.
- **BOM — Proyectos legacy sin RC/RI (`jefe_ingenieria=None`):** `modules/proyectos/service.py`. Se eliminó el fallback al primer jefe organizacional. Para proyectos anteriores al modelo RC/RI sin fila en `tb_proyecto_usuarios`, `jefe_ingenieria` y `jefe_construccion` son `None`. Los callers actuales tienen guards correctos, pero verificar antes de agregar nuevos callers que accedan a estos campos.
- **BOM — Eficiencia: `get_bom_proyecto` antes del 404 guard en `bom_ui`:** `core/bom/router.py` l.159-160. `get_bom_proyecto` se ejecuta antes de `get_proyecto_info` (que hace el 404). Invertir el orden: verificar que el proyecto existe primero y abortar barato antes de buscar el BOM.
- **SharePoint — `import re` dentro de método estático:** `core/integrations/sharepoint.py` l.236. `_sanitize_filename` hace `import re` en el cuerpo del método en lugar del top del módulo. Mover al encabezado del archivo.
